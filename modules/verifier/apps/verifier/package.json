{
  "name": "the-verifier-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verifier Agent — read-only second agent that verifies the builder's claims and prompts back with concrete corrective feedback.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.79.10",
    "@earendil-works/pi-tui": "0.79.10",
    "@types/node": "^22.0.0",
    "typebox": "^1.1.34",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
